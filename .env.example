# Docker Registry Configuration
# Defaults to Docker Hub if not specified
DOCKER_REGISTRY=docker.io
DOCKER_IMAGE_NAME=vasie-dev
IMAGE_TAG=latest

# For GitHub Container Registry, use:
# DOCKER_REGISTRY=ghcr.io
# DOCKER_IMAGE_NAME=ghcr.io/vasie-dev

# For custom registry, set your registry URL:
# DOCKER_REGISTRY=your-registry.com
