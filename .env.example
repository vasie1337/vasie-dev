# Docker Registry Configuration for GitHub Container Registry (GHCR)
# Replace 'vasie' with your GitHub username

IMAGE_NAME=ghcr.io/vasie/vasie-dev
IMAGE_TAG=latest

# To use a specific tag, change IMAGE_TAG:
# IMAGE_TAG=main-abc1234
# IMAGE_TAG=v1.0.0
# IMAGE_TAG=latest

# For local development, edit docker-compose.yml to use 'build' instead
